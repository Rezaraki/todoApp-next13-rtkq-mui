import { styled } from "@mui/system";
import Box from "@mui/material/Box";
export const VCenteredBox = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  //   alignItems: "center",
});
